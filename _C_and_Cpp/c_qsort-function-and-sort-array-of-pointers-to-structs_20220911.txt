filename: c_qsort-function-and-sort-array-of-pointers-to-structs_20220911.txt
https://www.anycodings.com/1questions/2711795/how-to-make-a-qsort-function-and-sort-array-of-pointers-to-structs

how to make a qsort function and sort array of pointers to structs
2022-09-08

   i'm trying to make a qsort function from anycodings_quicksort scratch that sorts an array of pointers
   to anycodings_quicksort structs

   this is the code i have right now

static void swap(int *a, int *b) {
	int tmp = *a;
	*a = *b;
	*b = tmp;
}

void _qsort(void* list, int list_len, int left, int right,
			int(*comp)(const struct shpg_item *a, const struct shpg_item *b)) {
	void *vt, *v3;
	int i, last, mid = (left + right) / 2;
	if (left >= right) {
		return;
	}

	void* vl = (char*)(list + (left * list_len));
	void* vr = (char*)(list + (mid * list_len));
	swap(vl, vr);
	last = left;

	for (i = left + 1; i <= right; i++) {
		// vl and vt will have the starting address
		// of the elements which will be passed to
		// comp function.
		vt = (char*)(list + (i * list_len));
		if ((*comp)(vl, vt) > 0) {
			++last;
			v3 = (char*)(list + (last * list_len));
			swap(vt, v3);
		}
	}

	v3 = (char*)(list + (last * list_len));
	swap(vl, v3);
	_qsort(list,list_len, left, last - 1, comp);
	trace_int(1);
	_qsort(list, list_len, last + 1, right, comp);
}

void list_sort(struct shpg_item **list, int list_len,
			int(*comp)(const struct shpg_item *a, const struct shpg_item *b)) {
  _qsort(*list,list_len,0,(list_len-1),comp);
}

   but this gives a segmentation fault error , anycodings_quicksort can any one tell me why and help me?

***
Answers 1 : of how to make a qsort function and sort array of pointers to structs

void * pointer addition
   void * pointer addition is undefined anycodings_struct behavior. But since the usual UB is OK,
   anycodings_struct this may or may not be OP's trouble.

void _qsort(void* list, int list_len, int left, ...
	...
	(list + (left * list_len))	// UB

   Instead recommend casting before anycodings_struct addition.
// void* vl = (char*)(list + (left * list_len));
void* vl = ((char*) list) + (left * list_len);

Answers 2 : of how to make a qsort function and sort array of pointers to structs
   I haven't check the entire code but your anycodings_struct swap function seems wrong. Depending on
   anycodings_struct the comment lines in your code;
// vl and vt will have the starting address
// of the elements which will be passed to
// comp function.

   if (list + (left * list_len)) and (list anycodings_struct + (last * list_len)) are pointers to be
   anycodings_struct swapped (pointers to a string or a anycodings_struct struct, for example), your
   swap function anycodings_struct decoration & your caller line should anycodings_struct read as:
	1. Swapping two integers, floats, doubles, etc (in general swapping values only):

void swap(int *a, int *b) {
	int t = *a;
	*a = *b;
	*b = t;
}
...
int x = 5;
int y = 3;
swap(&x, &y);

	2. If you need to swap two pointers (a char * string or another type of pointer pointing to a
	   struct), you can just swap pointer values without swapping the content pointed in the actual
	   memory:

void swap(void **a, void **b) {
	void *t = *a;
	*a = *b;
	*b = t;
}
...
char *x = "some string";
char *y = "some other string";
swap(&x, &y);

Answers 3 : of how to make a qsort function and sort array of pointers to structs
   I've included a working example in the anycodings_struct middle part of this answer, and also
   anycodings_struct added an example using qsort.

   Taking a quick look at the code I see anycodings_struct problem here:
void _qsort(void* list, ...

   Since list is an array of pointers it anycodings_struct should be:
void _qsort(void** list, ...

   or
void _qsort(void* list[], ...

   With this declaration, pointer anycodings_struct arithmetic will not be an issue, for
   anycodings_struct example, list+3 == &list[3] == anycodings_struct pointer to the 3rd pointer in the
   array. anycodings_struct There's no need to cast list, as void** anycodings_struct list will work
   fine in the main part of anycodings_struct the code. The only code that will do any anycodings_struct
   casting is the caller's compare anycodings_struct function.

   You can choose to emulate qsort's anycodings_struct compare function parameters using type
   anycodings_struct void **: compare(list+i, list+j), but it anycodings_struct would be simpler to use
   type void *: anycodings_struct compare(list[i], list[j]).

   Swap should use void** as parameters. anycodings_struct The call would be
	swap(list+i, list+j)

/* ... */

void swap(void **i, void **j) {
	void * t;
	t = *i;
	*i = *j;
	*j = t;
}

   There are some comments about a void anycodings_struct pointer possibly having a different size
   anycodings_struct than a struct pointer or any type of anycodings_struct data pointer, and that this
   could cause anycodings_struct an issue. If this was true, then the C anycodings_struct library
   function qsort() would not work anycodings_struct because the first parameter for qsort is
   anycodings_struct a void pointer, which will result in the anycodings_struct caller's pointer being
   cast to a void anycodings_struct pointer. In the caller's compare anycodings_struct function, both
   parameters are const void anycodings_struct pointers which the caller's compare anycodings_struct
   function has to cast to the actual anycodings_struct pointer types. With qsort() and the
   anycodings_struct caller's compare function, parameters anycodings_struct are being cast both to and
   from void anycodings_struct pointers without issue.

   C guarantees that a void pointer can be anycodings_struct used to hold any type of data pointer,
   anycodings_struct so in essence a void pointer is a anycodings_struct generic data pointer (in 16 bit
   segment anycodings_struct or selector environments, a generic anycodings_struct "near" data pointer).

   This is a working example, using typical anycodings_struct Lomuto partition scheme (pivot = a[hi]):

#include <stdio.h>
#include <stdlib.h>

typedef struct {
	int  data;
	char name[32];
} XMPL;

int cmpr(void * pi, void *pj) {
	if(((XMPL *)pi)->data < ((XMPL *)pj)->data) {
		return -1;
	}

	if(((XMPL *)pi)->data > ((XMPL *)pj)->data) {
		return	1;
	}

	return 0;
}

void swap(void **i, void **j) {
	void * t;
	t = *i;
	*i = *j;
	*j = t;
}

void QuickSort(void **a, int lo, int hi, int(*cmpp)(void *, void *)) {
	void *p;
	int i, j;

	while(lo < hi) {
		p = a[hi];
		i = lo;
		for (j = lo; j < hi; ++j) {
			if((cmpp(a[j], p) < 0)) {
				swap(a+i, a+j);
				++i;
			}
		}

		swap(a+i, a+hi);
		if(i - lo <= hi - i){           /* avoid stack overflow */
			QuickSort(a, lo, i-1, cmpp);
			lo = i+1;
		} else {
			QuickSort(a, i+1, hi, cmpp);
			hi = i-1;
		}
	}
}

#define COUNT (1024)

int main(int argc, char**argv) {
XMPL *ax;                                /* array of structures */
XMPL **pax;                              /* array of pointers to structures */
int i;

	ax =  malloc(COUNT * sizeof(XMPL));
	pax = malloc(COUNT * sizeof(void **));

	for (i = 0; i < COUNT; i++) {         /* init structs, array of ptrs */
		ax[i].data = rand();
		pax[i] = ax+i;
	}

	QuickSort(pax, 0, COUNT-1, cmpr);

	for (i = 1; i < COUNT; i++) {
		if(pax[i-1]->data > pax[i]->data) {
			break;
		}
	}

	if(i == COUNT) {
		printf("passed\n");
	} else {
		printf("failed\n");
	}

	free(pax);
	free(ax);

	return(0);
}

   Hoare parition scheme will probably be a anycodings_struct bit faster. However, in this case, merge
   anycodings_struct sort should be faster than quick sort. anycodings_struct Merge sort does more moves
   but fewer anycodings_struct compares than quick sort, and in this anycodings_struct case, only
   pointers are being moved, anycodings_struct while the compare involves an anycodings_struct
   indirection via a pointer and a call to anycodings_struct a compare function via a pointer.

   Same basic code, but using qsort. Note anycodings_struct that the cmpr() function needed one more
   anycodings_struct dereference for each parameter.
#include <stdio.h>
#include <stdlib.h>

typedef struct {
	int  data;
	char name[32];
} XMPL;

int cmpr(const void * pi, const void *pj) {
	if((*(XMPL **)pi)->data < (*(XMPL **)pj)->data) {
		return -1;
	}

	if((*(XMPL **)pi)->data > (*(XMPL **)pj)->data) {
		return 1;
	}
	return 0;
}

#define COUNT (1024)

int main(int argc, char**argv) {
	XMPL *ax;                                /* array of structures */
	XMPL **pax;                              /* array of pointers to structures */
	int i;

	ax =  malloc(COUNT * sizeof(XMPL));
	pax = malloc(COUNT * sizeof(void **));

	for (i = 0; i < COUNT; i++) {            /* init structs, array of ptrs */
		ax[i].data = rand();
		pax[i] = ax+i;
	}

	qsort(pax, COUNT, sizeof(XMPL *), cmpr);

	for (i = 1; i < COUNT; i++){
		if(pax[i-1]->data > pax[i]->data){
			break;
		}
	}

	if(i == COUNT) {
		printf("passed\n");
	} else {
		printf("failed\n");
	}

	free(pax);
	free(ax);

	return(0);
}



---

